{
  "name": "repro-vitest-localstorage-mock-issue-2",
  "scripts": {
    "test:node": "vitest run",
    "test:dom": "vitest run --dom",
    "test:browser": "vitest run --browser",
    "test": "vitest run"
  },
  "devDependencies": {
    "happy-dom": "^13.6.2",
    "vitest": "^1.3.1",
    "vitest-localstorage-mock": "^0.1.2"
  }
}
